<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:amq="http://activemq.apache.org/schema/core"
       xmlns:camel="http://camel.apache.org/schema/spring"
       xmlns:jms="http://www.springframework.org/schema/jms"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                           http://activemq.apache.org/schema/core http://activemq.apache.org/schema/core/activemq-core.xsd
                           http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd
                           http://www.springframework.org/schema/jms http://www.springframework.org/schema/jms/spring-jms.xsd">

    <context:component-scan base-package="org.gislers.esb.product"/>

    <bean id="amqCxF" class="org.apache.activemq.ActiveMQConnectionFactory">
        <property name="brokerURL">
            <value>failover:(tcp://localhost:61616,tcp://localhost:61626)</value>
        </property>
    </bean>

    <bean id="pooledCxF" class="org.apache.activemq.pool.PooledConnectionFactory" destroy-method="stop">
        <property name="connectionFactory" ref="amqCxF"/>
        <property name="maxConnections" value="20"/>
    </bean>

    <amq:queue id="product.esb.inbound.queue" physicalName="product.esb.inbound.queue"/>
    <amq:topic id="product.esb.outbound.topic" physicalName="outbound.esb.product.info.topic"/>

    <bean id="inboundProductNotifyTemplate" class="org.springframework.jms.core.JmsTemplate">
        <property name="connectionFactory" ref="pooledCxF"/>
        <property name="defaultDestination" ref="product.esb.inbound.queue"/>
    </bean>

    <bean id="activemq" class="org.apache.activemq.camel.component.ActiveMQComponent">
        <property name="connectionFactory" ref="pooledCxF"/>
    </bean>

    <camel:camelContext>
        <camel:jmxAgent id="camelJmx" createConnector="true"/>
        <camel:route>
            <camel:from uri="activemq:queue:product.esb.inbound.queue"/>
            <camel:to uri="activemq:topic:product.esb.outbound.topic"/>
        </camel:route>
    </camel:camelContext>

    <jms:listener-container acknowledge="transacted" connection-factory="pooledCxF" client-id="consumerA"
                            cache="connection" destination-type="durableTopic">
        <jms:listener destination="product.esb.outbound.topic" ref="consumerA" method="onMessage"/>
    </jms:listener-container>

    <jms:listener-container acknowledge="transacted" connection-factory="pooledCxF" client-id="consumerB"
                            cache="connection" destination-type="durableTopic">
        <jms:listener destination="product.esb.outbound.topic" ref="consumerB" method="onMessage"/>
    </jms:listener-container>

</beans>